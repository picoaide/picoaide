version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
  exclude-dirs:
    - picoaide-desktop
    - picoaide-extension
    - website
