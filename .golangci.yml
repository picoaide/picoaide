version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - errcheck
    - staticcheck
    - unused

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
