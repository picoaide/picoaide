version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - gocritic
  settings:
    gocritic:
      enabled-checks:
        - dupImport
        - truncateCmp
        - unnamedResult

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
